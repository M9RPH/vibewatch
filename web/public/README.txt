Branding asset location
=======================

Place the Vibewatch logo supplied by the project owner at:

  web/public/logo.png

Vite copies it to /logo.png and the UI automatically uses it on login/sidebar.
If the file is absent or cannot be loaded, the interface falls back to the built-in W tile.
